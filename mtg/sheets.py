"""
Google Sheets sync — to be implemented.

Dependencies (add to requirements.txt when ready):
    gspread>=6.0
    google-auth>=2.0

Usage pattern (future):
    from mtg.sheets import push_to_sheets
    push_to_sheets(spreadsheet_id="...", db_path=DB_PATH)
"""


def push_to_sheets(spreadsheet_id: str, db_path=None) -> None:
    raise NotImplementedError("Google Sheets sync not yet implemented")
